spring:
  application:
    name: MicroservizioBase

  # Il tastino qui permette di fare la connessione al Data Base
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: Strato_29
    url: jdbc:mysql://localhost:3306/chatdb
    username: edoardo
  jpa:
    hibernate:
      ddl-auto:
        update
      dialect:
        org.hibernate.dialect.MySQL8Dialect